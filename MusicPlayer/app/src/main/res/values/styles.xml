<resources xmlns:tools="http://schemas.android.com/tools">

    <style name="Theme.mPlayer" parent="Theme.mPlayer.Base" />

    <style name="Theme.mPlayer.Light" parent="Theme.mPlayer.Base.Light" />

    <style name="Theme.mPlayer.Black" parent="Theme.mPlayer.Base.Black" />

    <style name="Theme.mPlayer.Notification" parent="@android:style/TextAppearance.StatusBar.EventContent" />

    <style name="Theme.mPlayer.Notification.Title" parent="@android:style/TextAppearance.StatusBar.EventContent.Title" />

    <style name="OverFlowButton">
        <item name="srcCompat">@drawable/ic_more_vert_white_24dp</item>
        <item name="android:layout_margin">8dp</item>
        <item name="android:layout_width">@dimen/_40sdp</item>
        <item name="android:layout_height">@dimen/_40sdp</item>
        <item name="android:scaleType">center</item>
        <item name="android:background">?attr/roundSelector</item>
        <item name="android:focusableInTouchMode">false</item>
        <item name="android:focusable">false</item>
    </style>

    <style name="TabTextAppearance" parent="TextAppearance.Design.Tab">
        <item name="textAllCaps">false</item>
        <item name="android:textAllCaps">false</item>
        <item name="android:textColor">#40FFFFFF</item>
        <item name="android:gravity">center</item>
        <item name="android:textSize">@dimen/_13sdp</item>
        <item name="fontFamily">@font/roboto_medium</item>
    </style>

    <style name="Fab" parent="FabParent" />

    <style name="MusicProgressSlider" parent="MusicProgressSliderParent">
        <item name="android:thumbOffset">0dp</item>
    </style>

    <style name="Toolbar">
        <item name="titleMarginStart">16dp</item>
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">?android:attr/actionBarSize</item>
        <item name="android:theme">@style/ThemeOverlay.AppCompat.Dark.ActionBar</item>
        <item name="popupTheme">?toolbarPopupTheme</item>
        <item name="titleTextAppearance">@style/ToolbarText</item>
        <item name="android:elevation" tools:ignore="NewApi">@dimen/toolbar_elevation</item>
    </style>

    <style name="ToolbarText" parent="ThemeOverlay.AppCompat">
        <item name="android:textStyle">bold</item>
        <item name="itemBackground">@drawable/bg_popup</item>
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">?android:attr/actionBarSize</item>
        <item name="android:textSize">@dimen/_15sdp</item>
        <item name="fontFamily">@font/roboto_bold</item>
    </style>

    <style name="CustomDialog" parent="@android:style/Theme.Dialog">
        <item name="android:windowMinWidthMajor">95%</item>
        <item name="android:windowMinWidthMinor">95%</item>

        <item name="android:windowAnimationStyle">@style/DialogAnimation</item>
    </style>

    <style name="DialogAnimation">
        <item name="android:windowEnterAnimation">@anim/slide_up</item>
        <item name="android:windowExitAnimation">@anim/slide_down</item>
    </style>
</resources>
